{
    "authenticationMethod": "route",
    "routes": [
        {
            "source": "^/sales-order/(.*)",
            "destination": "srv-api",
            "csrfProtection": true,
            "authenticationType": "xsuaa"
        }
    ]
}